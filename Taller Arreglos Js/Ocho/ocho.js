//8. Cree una función que reciba un arreglo y retorne su longitud(número de elementos)

let arreglo = ["giant", "mari", "leon", "Antonella", 1, 10, 21]

function recibir() {
    return arreglo.length
}
let retorno = recibir();
console.log(retorno); 